# Copy to .env.local and fill in from your Supabase project (Settings → API).
# The app works fully offline/local without these — they only enable cloud sync.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
